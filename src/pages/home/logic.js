const course1 = () => {
    var classList = document.querySelectorAll(".input-value-1")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }     
        
}

const course2 = () => {
    var classList = document.querySelectorAll(".input-value-2")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }        
        
}
const course3 = () => {
    var classList = document.querySelectorAll(".input-value-3")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }        
        
}
const course4 = () => {
    var classList = document.querySelectorAll(".input-value-4")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }        
        
}
const course5 = () => {
    var classList = document.querySelectorAll(".input-value-5")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }        
        
}
const course6 = () => {
    var classList = document.querySelectorAll(".input-value-6")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }        
        
}
const course7 = () => {
    var classList = document.querySelectorAll(".input-value-7")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }        
        
}
const course8 = () => {
    var classList = document.querySelectorAll(".input-value-8")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }        
        
}
const course9 = () => {
    var classList = document.querySelectorAll(".input-value-9")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }        
        
}
const course10 = () => {
    var classList = document.querySelectorAll(".input-value-10")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }        
        
}
const course11 = () => {
    var classList = document.querySelectorAll(".input-value-11")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }        
        
}
const course12 = () => {
    var classList = document.querySelectorAll(".input-value-12")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }        
        
}
const course13 = () => {
    var classList = document.querySelectorAll(".input-value-13")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }        
        
}
const course14 = () => {
    var classList = document.querySelectorAll(".input-value-14")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }        
        
}
const course15 = () => {
    var classList = document.querySelectorAll(".input-value-15")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        
          if (sum >= 70) {
            return 5;
          }
          if (sum >= 60) {
            return 4;
          }
          if (sum >= 50) {
            return 3;
          }
          if (sum >= 45) {
            return 2;
          }
          if (sum < 45) {
            return 0;
          }        
        
}

const tUnit = () => {
    var classList = document.querySelectorAll(".input-unit")
        const main = Array.prototype.map.call(classList, function(element){
            return element.value;  
          })
          let bbc = n => isNaN(n) ? 0 : n;

        var sum = main.reduce(function(a, b) { return bbc(parseInt(a)) + bbc(parseInt(b));}, 0);
        return sum;
}

export const total = () => {
    const c1 = course1()
    const c2 = course2()
    const c3 = course3()
    const c4 = course4()
    const c5 = course5()
    const c6 = course6()
    const c7 = course7()
    const c8 = course8()
    const c9 = course9()
    const c10 = course10()
    const c11 = course11()
    const c12 = course12()
    const c13 = course13()
    const c14 = course14()
    const c15 = course15()

    const  _sum  = tUnit()

    const cTotal = (c1 + c2 + c3 + c3 + c4 + c5 + c6 + c7 + c8 + c9 + c10 + c11 + c12 + c13 + c14 +c15) / _sum
    return cTotal;
}